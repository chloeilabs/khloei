name: Verify Khloei

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: khloei-verify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          cache: npm
          node-version: 24.15.0

      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.11

      - name: Install application dependencies
        run: npm ci

      - name: Install computer dependencies
        run: bun install --cwd services/computer --frozen-lockfile

      - name: Install agent worker dependencies
        run: bun install --cwd services/agent-worker --frozen-lockfile

      - name: Verify application and services
        run: npm run check
